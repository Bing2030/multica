// Auth views were removed under the DevBypass POC — there is no login form,
// CLI callback, or logout UI. The barrel is kept empty so `@multica/views/auth`
// stays a valid import path for any leftover references during the transition.
// THROWAWAY POC: NEVER MERGE.
export {};